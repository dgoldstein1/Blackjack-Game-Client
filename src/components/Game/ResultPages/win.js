// Import
import React from "react";

const Win = props => {
  // Destructure
  //   const { image, suit, value } = props.card;

  // Display
  return (
    <div>
      <h1>Win</h1>
    </div>
  );
};

export default Win;
