// Import
import React from "react";

const Draw = props => {
  // Destructure
  //   const { image, suit, value } = props.card;

  // Display
  return (
    <div>
      <h1>Draw</h1>
    </div>
  );
};

export default Draw;
