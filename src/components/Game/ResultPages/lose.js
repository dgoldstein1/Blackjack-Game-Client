// Import
import React from "react";

const Lose = props => {
  // Destructure
  //   const { image, suit, value } = props.card;

  // Display
  return (
    <div>
      <h1>Lose</h1>
    </div>
  );
};

export default Lose;
